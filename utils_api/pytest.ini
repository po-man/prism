[pytest]
; This file configures pytest for the utils_api project.

; Set environment variables for the entire test run, including the collection phase.
; This is the most reliable way to ensure environment-dependent modules load correctly.
; D: means the variable is set for the test run and does not override existing env vars.
env =
    D:SCHEMA_DIR=../schemas

addopts = -ra -q